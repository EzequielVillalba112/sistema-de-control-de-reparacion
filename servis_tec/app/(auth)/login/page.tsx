import LoginForm from "@/components/ui/Login";

const Login = () => {
  return (
    <section className="w-full h-dvh flex flex-col relative bg-gray-950 items-center justify-center">
        <LoginForm/>
    </section>
  );
};

export default Login;
